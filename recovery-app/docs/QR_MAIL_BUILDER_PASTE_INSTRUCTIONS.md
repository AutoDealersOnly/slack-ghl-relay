# QR Mail Builder paste instructions — superseded

Do **not** use the earlier full-replacement code. It removed the page’s protected connection and caused a 401 error.

Use the verified append-only code and instructions instead:

- `docs/qr-mail-builder/qr-mail-builder-two-qr-add-on.html`
- `QR_MAIL_BUILDER_REBUILD.md`
- `QR_MAIL_BUILDER_SURGICAL_ADDON.md`

The verified September 25, 2026 method is to paste the complete secret-free add-on **at the absolute end** of the existing working code in **QR Dashboard Clone**. Do not replace or edit the original code.
